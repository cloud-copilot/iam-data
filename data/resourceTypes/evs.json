{
  "environment": {
    "key": "environment",
    "arn": "arn:${Partition}:evs:${Region}:${Account}:environment/${EnvironmentIdentifier}",
    "conditionKeys": [
      "aws:ResourceTag/${TagKey}"
    ]
  }
}