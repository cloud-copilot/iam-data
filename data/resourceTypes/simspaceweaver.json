{
  "simulation": {
    "key": "Simulation",
    "arn": "arn:${Partition}:simspaceweaver:${Region}:${Account}:simulation/${SimulationName}",
    "conditionKeys": [
      "aws:ResourceTag/${TagKey}"
    ]
  }
}