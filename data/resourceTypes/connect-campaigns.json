{
  "campaign": {
    "key": "campaign",
    "arn": "arn:${Partition}:connect-campaigns:${Region}:${Account}:campaign/${CampaignId}",
    "conditionKeys": [
      "aws:ResourceTag/${TagKey}"
    ]
  }
}