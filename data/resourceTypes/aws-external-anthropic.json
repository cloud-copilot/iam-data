{
  "workspace": {
    "key": "workspace",
    "arn": "arn:${Partition}:aws-external-anthropic:${Region}:${Account}:workspace/${ResourceId}",
    "conditionKeys": [
      "aws:ResourceTag/${TagKey}"
    ]
  }
}