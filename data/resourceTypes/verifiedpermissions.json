{
  "policy-store": {
    "key": "policy-store",
    "arn": "arn:${Partition}:verifiedpermissions::${Account}:policy-store/${PolicyStoreId}",
    "conditionKeys": [
      "aws:ResourceTag/${TagKey}"
    ]
  }
}