{
  "workflow": {
    "key": "Workflow",
    "arn": "arn:${Partition}:airflow-serverless:${Region}:${Account}:workflow/${WorkflowId}",
    "conditionKeys": [
      "aws:ResourceTag/${TagKey}"
    ]
  }
}