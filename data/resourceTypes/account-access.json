{
  "application": {
    "key": "application",
    "arn": "arn:${Partition}:account-access:${Region}:${Account}:application/${ResourceId}",
    "conditionKeys": [
      "aws:ResourceTag/${TagKey}"
    ]
  }
}