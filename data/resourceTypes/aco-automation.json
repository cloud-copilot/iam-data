{
  "automationrule": {
    "key": "AutomationRule",
    "arn": "arn:${Partition}:compute-optimizer::${Account}:automation-rule/${RuleId}",
    "conditionKeys": [
      "aws:ResourceTag/${TagKey}"
    ]
  }
}