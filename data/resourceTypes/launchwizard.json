{
  "deployment": {
    "key": "deployment",
    "arn": "arn:${Partition}:launchwizard:${Region}:${Account}:deployment/${DeploymentId}",
    "conditionKeys": [
      "aws:ResourceTag/${TagKey}"
    ]
  }
}