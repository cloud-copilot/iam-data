{
  "emailcontactresource": {
    "key": "EmailContactResource",
    "arn": "arn:${Partition}:notifications-contacts::${Account}:emailcontact/${EmailContactId}",
    "conditionKeys": [
      "aws:ResourceTag/${TagKey}"
    ]
  }
}